{
  "name": "adverlink",
  "version": "1.0.0",
  "description": "Playwright project",
  "main": "playwright/test.js",
  "scripts": {
    "test": "node playwright/test.js"
  },
  "dependencies": {
    "playwright": "^1.45.3"  // Ensure this is the correct version
  },
  "engines": {
    "node": "20.x"
  }
}
